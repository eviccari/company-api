spring:
    datasource:
        url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/company
        username: company_user_service
        password: 123
        driver-class-name: com.mysql.cj.jdbc.Driver

server:
    port: 8080
    servlet:
        contextPath: / 
